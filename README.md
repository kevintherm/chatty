# Chatty

Free-for-all chat app with E2E encryption. Uses [VeloquentPHP](https://velophp.com) as the backend.

## Installation

1. Clone
2. Run `dev` script
3. Setup Veloquent backend, import the backend schema
4. Run `realtime:worker` and setup your realtime driver
5. Done

